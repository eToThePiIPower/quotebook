<div class="container">
  <%= render 'errors' %>

  <h1>Editing Quote</h1>

  <div id="quote-form" class="card">
    <div class="card-body">
      <%= render 'form', quote: @quote %>
    </div>

    <div class="card-footer">
      <%= link_to 'Show', @quote %> |
      <%= link_to 'Back', quotes_path %>
    </div>
  </div>
</div>
